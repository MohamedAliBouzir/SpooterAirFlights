import type { SxProps, Theme } from "@mui/material";

export const LoadingInterfaceStyle: SxProps<Theme> = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    width: "100%",
};

export const LoadingScreenStyle = LoadingInterfaceStyle;
