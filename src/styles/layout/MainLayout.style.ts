import type { SxProps, Theme } from '@mui/material/styles';

export const mainLayoutStyles = {
    root: {
        display: 'flex',
        flexDirection: 'column',
        minHeight: '100vh',
        width: '100%'
    },
    main: {
        flexGrow: 1,
        width: '100%',
        display: 'flex',
        flexDirection: 'column'
    }
};
